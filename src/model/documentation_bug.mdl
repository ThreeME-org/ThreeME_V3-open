# Ligne de commande utilisé pour le test:
# teXdoc(c("adjustments.mdl", "documentation_bug.mdl"), exo = c("exogenous.mdl"), out = "ThreeMEv3_eqs_01")

##### Housing

#### Link with the rest of the model

# La specification suivante ne marche pas:

# ##! Necessary (minimum) households' final consumption for construction commodity $ccon$
# NCH[ccon] = (@year > %baseyear) * (@elem(PNewBUIL,%baseyear) * NewBUIL + @elem(PREHAB,%baseyear) * REHAB) _
#               + (@year <= %baseyear) * (NCH[ccon]{-1} * ( 1 + GR_REAL ) )


# Raisons:
# 1- le compilateur ne reconnait pas "_" qui passe à la ligne. En effet voir bug généré par:   


# ##! Necessary (minimum) households' final consumption for construction commodity $ccon$
# NCH[ccon] = (@elem(PNewBUIL,%baseyear) * NewBUIL + @elem(PREHAB,%baseyear) * REHAB) _
#                + (NCH[ccon]{-1} * ( 1 + GR_REAL ) )

# Alors que la ligne suivante fonctionne:

# ##! Necessary (minimum) households' final consumption for construction commodity $ccon$
# NCH[ccon] = (@elem(PNewBUIL,%baseyear) * NewBUIL + @elem(PREHAB,%baseyear) * REHAB)  + (NCH[ccon]{-1} * ( 1 + GR_REAL ) )


# 2- le compilateur ne reconnait pas: (@year <= %baseyear). En effet voir bug généré par:   

# ##! Necessary (minimum) households' final consumption for construction commodity $ccon$
# NCH[ccon] = (@year > %baseyear) * (@elem(PNewBUIL,%baseyear) * NewBUIL + @elem(PREHAB,%baseyear) * REHAB) + (@year <= %baseyear) * (NCH[ccon]{-1} * ( 1 + GR_REAL ) )

# Par contre (@year > %baseyear) est bien lu. Voir essaie ci-dessous. 

# ##! Necessary (minimum) households' final consumption for construction commodity $ccon$
# NCH[ccon] = (@year > %baseyear) * (@elem(PNewBUIL,%baseyear) * NewBUIL + @elem(PREHAB,%baseyear) * REHAB)

# Mais il serait préférable d'omettre toutes les conditionnalités de temps qui sont purement techniques pour le sentier équilibré car elles alourdissent inutilement la syntaxe.
